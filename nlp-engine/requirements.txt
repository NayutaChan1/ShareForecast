fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Database
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Messaging
pika==1.3.2

# Scraping / scheduling
feedparser==6.0.11
APScheduler==3.11.0

# Market data
# 1.x is required: Yahoo now rejects plain `requests` traffic with an HTML
# block page, and only these versions impersonate a browser via curl_cffi.
# curl_cffi comes in as a yfinance dependency; pinning it here only creates
# a resolver conflict, so let yfinance choose its own compatible version.
yfinance==1.6.0

# AI inference
transformers==4.47.1
torch==2.5.1
